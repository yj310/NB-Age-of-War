/// --------------[Interaction]--------------

function mousePressed() {
    /// 마우스 입력
    /// mouseX, mouseY
}

function keyPressed() {
    /// 키보드 입력
    // if (key === '') { }
    // if (keyCode === LEFT_ARROW) {}
}

