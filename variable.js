class Frame {
    constructor(color, width, height) {
        this.color = color;
        this.width = width;
        this.height = height;
    }
}

/// 전체 배경
let foundation = null;

/// 게임 화면
let gameScreen = null;
